apiVersion: 1

datasources:
  - name: Prometheus
    # UID must match dashboard.DatasourceUID so the generated panels bind.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
